@ECHO OFF

:: Tente evitar espaços, caracteres especiais pois BATCH usa ANSI. A extensão é .bat ou .cmd. Não nomeie também com nomes no sistema, tais como ping.cmd.

:: Para executar um arquivo batch, basta ir até onde o arquivo está e clicar duas vezes no arquivo, ou via console DOS, digitar o nome do arquivo.

:: Para modificar um arquivo batch, é da mesma forma que um arquivo de texto.

PAUSE > NUL